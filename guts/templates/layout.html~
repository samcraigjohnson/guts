<!doctype html>
<title>Guts</title>
<div class=page>
  <h1>B-ball</h1>
  {% for message in get_flashed_messages %}
     <div class=flash>{{ message }}</div>
  {% endfor %}
  {% block body %}{% endblock %}
</div>
